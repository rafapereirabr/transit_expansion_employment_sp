## Urban Transport and Employment - The case of São Paulo


To run this project, simply run `targets::tar_make()`.

> File `_wizard.R` contains shortcuts to use targets, e.g. directed `tar_make` calls, VisNetwork and so on.

